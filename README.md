# STOCK-PRICE-PREDICTION-USING-LSTM
 A deep learning project that uses Long Short-Term Memory (LSTM) networks to predict future stock prices based on historical data. The model captures temporal patterns in time series data and provides visual comparisons between actual and predicted prices. Built using Python libraries like TensorFlow, Pandas, and Matplotlib.
